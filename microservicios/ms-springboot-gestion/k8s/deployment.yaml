apiVersion: apps/v1
kind: Deployment
metadata:
  name: ms-springboot-gestion
  labels:
    app: ms-springboot-gestion
spec:
  replicas: 1
  # Nodo unico con CPU al limite: Recreate evita el pod extra (surge) del
  # RollingUpdate, que no cabe y deja el deploy colgado en Pending.
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: ms-springboot-gestion
  template:
    metadata:
      labels:
        app: ms-springboot-gestion
    spec:
      containers:
        - name: ms-springboot-gestion
          # __IMAGE__ lo reemplaza el pipeline de GitHub Actions
          image: __IMAGE__
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: ms-springboot-gestion-config
            - secretRef:
                name: ms-springboot-gestion-secret
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 60
            periodSeconds: 20
